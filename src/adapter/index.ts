import { api, client, getAvailableTimeZones, getConfig, getProductIdentificationPref, getUserFacilities, getUserPreference, hasError, initialise, logout, resetConfig, setProductIdentificationPref, setUserLocale, setUserPreference, setUserTimeZone, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  getAvailableTimeZones,
  getConfig,
  getProductIdentificationPref,
  getUserFacilities,
  getUserPreference,
  hasError,
  initialise,
  logout,
  resetConfig,
  setProductIdentificationPref,
  setUserLocale,
  setUserPreference,
  setUserTimeZone,
  updateInstanceUrl,
  updateToken
}